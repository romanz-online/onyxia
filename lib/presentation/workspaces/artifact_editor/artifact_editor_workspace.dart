import 'package:onyxia/export.dart';

class ArtifactEditorWorkspace extends ConsumerWidget {
  const ArtifactEditorWorkspace({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) => const ArtifactEditor();
}
